{
  "name": "escape-code",
  "version": "0.0.1",
  "description": "ANSI Escape Code as an object",
  "license": "MIT",
  "repository": "jamestalmage/escape-code",
  "author": {
    "name": "James Talmage",
    "email": "james@talmage.io",
    "url": "github.com/jamestalmage"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "npm run cover && npm run lint && npm run style",
    "cover": "istanbul cover ./node_modules/.bin/_mocha",
    "lint": "jshint *.js lib/*.js lib/**/*.js test/*.js test/**/*.js",
    "debug": "mocha",
    "watch": "mocha -w",
    "style": "jscs *.js ./**/*.js && jscs ./test/** --config=./test/.jscsrc"
  },
  "files": [
    "index.js"
  ],
  "keywords": [],
  "dependencies": {
    "delegates": "^0.1.0"
  },
  "devDependencies": {
    "codeclimate-test-reporter": "^0.1.0",
    "coveralls": "^2.11.2",
    "istanbul": "^0.3.17",
    "jscs": "^1.13.1",
    "jshint": "^2.8.0",
    "mocha": "^2.2.5"
  }
}
